{% extends 'base.html.twig' %}

{% block title %}Conferences{% endblock %}

{% block body %}
<ul class="list-group">
    {% for conference in conferences %}
            <li class="list-group-item"><a href="{{ path('conference', {id: conference.id}) }}"> {{ conference }}</a></li>
    {% endfor %}
</ul>
{% endblock %}
